# v1.1.0 — 2026-03-26

## What's new

- Initial MoneyTracker Telegram bot implementation

## Bug fixes

- Remove Markdown parse mode from /start to fix silent send failure
- Address HIGH and MEDIUM issues from go-reviewer
- Handle all unchecked error returns caught by golangci-lint

## Internal changes

- Copy deploy.sh to server and run as file instead of bash -s
- Avoid if ! fn pattern that triggers set -e on negated exit code
- Log service start and done on postgres timeout failure path
- Wrap postgres wait loop in function to avoid set -e on while condition
- Replace for/break with while loop to fix set -e bash bug
- Use ready flag to fix set -e interaction in postgres wait loop
- Replace 'if !' with if/else for goose to avoid set -e interaction
- Remove sudo from docker compose calls in deploy.sh
- Add set -x to deploy.sh for debug output
- Replace scp with tar|ssh and upgrade ssh-agent to v0.9.1
- Upgrade golangci-lint-action to v9 (Node.js 24 support)
- Upgrade Go to 1.26.1 to fix 21 stdlib CVEs
- Upgrade golangci-lint-action to v7 for golangci-lint v2 support
- Fix golangci-lint version format and upgrade actions to Node.js 24
- Fix yaml syntax by moving secrets to env vars
- Full CI/CD pipeline refactor with visibility and rollback
- Upgrade actions to Node.js 24 and enforce lint-before-test
- Split CI into test/lint jobs and fix goose installation
- Fix postgres health check to use correct user and database
- Build goose binary in CD instead of installing on server
- Add go.sum and fix invalid indirect dependency versions
- Automate server setup and docker compose in CD pipeline

## Notes

No breaking changes.
